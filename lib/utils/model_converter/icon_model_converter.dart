import 'package:flutter/widgets.dart';

class IconModelConverter {
  static IconData convertIconForModel(IconData icon) => icon;
  static IconData? convertIconOptionalForModel(IconData? icon) => icon;
}
